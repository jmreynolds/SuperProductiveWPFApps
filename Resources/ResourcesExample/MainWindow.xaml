<Window x:Class="ResourcesExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <FontFamily x:Key="StandardFontFace">Snap ITC</FontFamily>
    </Window.Resources>
    <Grid>
        <Grid.Resources>
            
        </Grid.Resources>
        <Button FontSize="{StaticResource StandardFontSize}"
                VerticalAlignment="Top" FontFamily="{DynamicResource StandardFontFace}" Background="{DynamicResource StandardButtonBackgroundColor}">Hello World!</Button>
        <TextBox FontSize="{StaticResource StandardFontSize}" VerticalAlignment="Top" Margin="0,100,0,0" FontFamily="{DynamicResource StandardFontFace}" Text="sdfsdsdfsfd" />
    </Grid>
</Window>
